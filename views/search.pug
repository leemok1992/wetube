extends layout/main
include mixin/videoBlock

block content
    .search_header
        h3 searching by #{searchingBy}
    .sesrch_video
        each video in videos
                +videoBlock({
                    title: video.title,
                    videofile: video.videofile
                })    